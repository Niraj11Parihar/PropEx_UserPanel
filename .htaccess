RewriteEngine On

# Redirect to public directory
RewriteRule ^public/ - [L]

# Redirect all requests to index if not a real file
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]